# nanobanana-style-copy-paste

Projeto TypeScript com pnpm.

## Requisitos

- Node.js (recomendado LTS)
- pnpm

## Como usar

```bash
pnpm install
pnpm run build
pnpm run start
```

## Scripts

- `pnpm run build` compila o TypeScript
- `pnpm run start` executa o código compilado

## Estrutura

- `src/index.ts` entrada principal
